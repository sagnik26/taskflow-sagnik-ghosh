# For local development: full deps + tsx watch (see entrypoint DEV_WATCH=1).

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    environment:
      NODE_ENV: development
      DEV_WATCH: "1"
